import React from 'react';
import { Stack, Text } from '@chakra-ui/react';
import Nav from '../Components/Nav/Nav';

const Home = () => {
  return (
    <Stack>
      <Nav />
    </Stack>
  );
};

export default Home;
